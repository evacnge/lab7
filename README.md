# lab7
**Lab Title:** Logistic Regression (Lab 7)

**Student Name:** Jafi Fahad Kanan
**Student ID:** 2240004698

**Summary:**
In this lab, we applied Logistic Regression to analyze advertising data and predict whether a user will click on an ad. First, we loaded and explored the dataset using basic functions such as `head()`, `info()`, and `describe()`. Then, we performed exploratory data analysis (EDA) using visualization tools like histograms and joint plots to understand relationships between variables.

After that, we prepared the data by selecting relevant features and splitting it into training and testing sets. We trained a Logistic Regression model on the training data and used it to make predictions on the test data. Finally, we evaluated the model using a classification report, achieving good accuracy.

**Conclusion:**
The model performed well in predicting user behavior, demonstrating the effectiveness of Logistic Regression for classification tasks.
